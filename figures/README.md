# Publication figures

These PNG and PDF panels are generated from the published result tables by
`analysis/summarize_results.py`. See `docs/results.md` for interpretation and
`docs/reproducibility.md` for the regeneration command.
